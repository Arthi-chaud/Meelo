import { WideLoadingComponent } from "./loading";

/**
 * Component to render when loading an Item component
 */
const LoadingItemComponent = () =>
	<WideLoadingComponent verticalPadding={5}/>;

export default LoadingItemComponent;
