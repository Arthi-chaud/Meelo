import Library from "../../models/library";

/**
 * Dummy library object for 'all' library
 */
const globalLibrary: Library = { name: 'All', slug: '', id: -1, path: '' };

export default globalLibrary;
